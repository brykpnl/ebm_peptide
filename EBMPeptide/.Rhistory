library(PeptideEBM)
library(PeptideEBM)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- PeptideEBM::analyze_data(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@ContrastData[['noUV-UV']]
head(analysis@ContrastData[['UV-noUV']], 10)
library(PeptideEBM)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- PeptideEBM::analyze_data(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@Pvalues[['noUV-UV']]
head(analysis@Pvalues[['UV-noUV']], 10)
library(roxygen2)
roxygen2::roxygenise()
roxygen2::roxygenise()
library(PeptideEBM)
library(PeptideEBM)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- ebm_analysis(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@Pvalues[['noUV-UV']]
head(analysis@Pvalues[['UV-noUV']], 10)
CA <- analysis@PValues[['C-A']]
View(analysis)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/CPTAC_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("A", "A", "A", "B", "B", "B", "C", "C", "C"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("B-A",
"C-B",
"C-A")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- ebm_analysis(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@PValues[['B-A']], analysis@PValues[['C-B']], and analysis@PValues[['C-A']]
CA <- analysis@PValues[['C-A']]
View(analysis)
analysis@Pvalues
CA <- analysis@PValues[['C-A']]
CA <- analysis@Pvalues[['C-A']]
head(CA[order(CA$p_right),])
head(CA[order(CA$p_right),], 10)
CA <- analysis@Pvalues[['C-A']]
head(CA[order(CA$p_right),], 10)
pvals_UV <- analysis@Pvalues[["UV-noUV"]]
head(pvals_UV[order(pvals_UV$p_right),], 10)
View(analysis)
pvals_CA <- analysis@Pvalues[["C-A"]]
head(pvals_CA[order(pvals_CA$p_right),], 10)
View(pvals_CA)
library(PeptideEBM)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- ebm_analysis(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@Pvalues[['noUV-UV']]
pvals_UV <- analysis@Pvalues[["UV-noUV"]]
head(pvals_UV[order(pvals_UV$p_right),], 10)
library(PeptideEBM)
?ebm_analysis
library(PeptideEBM)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- ebm_analysis(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@Pvalues[['noUV-UV']]
pvals_UV <- analysis@Pvalues[["UV-noUV"]]
head(pvals_UV[order(pvals_UV$p_right),], 10)
typeof(pvals_UV)
library(PeptideEBM)
?ebm_analysis
library(PeptideEBM)
?ebm_analysis
library(PeptideEBM)
library(PeptideEBM)
library(EBMPeptide)
detach("PeptideEBM")
detach("package:PeptideEBM")
gwtwd()
getwd()
library(EBMPeptide)
?ebm_analysis
source('~/Documents/Work/Projects/EBMPeptide/EBMPeptide/inst/examples/CPTAC.R')
head(pvals_CA[order(pvals_CA$p_right),], 10)
library(EBMPeptide)
set.seed(1)
#Path to .csv file containing peptide-level intensity data
data_path <- "./inst/testdata/GST_peptides.csv"
#Group data for peptide-level intensity data by sample (column)
groups <- factor(c("noUV", "noUV", "noUV", "UV", "UV", "UV"))
#Contrasts for group comparison. Note that this implementation only performs two-sample comparisons.
comparisons=c("UV-noUV")
#Analyze for differentially abundant proteins using eBayes + EBM
analysis <- ebm_analysis(data_path, groups, comparisons, min_obs=3, log2_transform=TRUE)
#p-value data.frames are now stored in analysis@Pvalues[['noUV-UV']]
pvals_UV <- analysis@Pvalues[["UV-noUV"]]
head(pvals_UV[order(pvals_UV$p_right),], 10)
library(EBMPeptide)
